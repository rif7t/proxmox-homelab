apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pm-homelab-root-api
  namespace: argocd
spec:
  project: management
  source:
    repoURL: https://github.com/rif7t/proxmox-homelab.git
    targetRevision: HEAD
    path: kubernetes/bootstrap
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true